{
  "name": "thai-report-ai",
  "version": "1.0.0",
  "private": true,
  "description": "AI assistant for Thai report generation with optional PrinceXML PDF export.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "lint": "node --check app.js && node --check server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
